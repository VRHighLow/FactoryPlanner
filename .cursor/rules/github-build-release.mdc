---
description: When the user says "build", cut a GitHub release with Win/Linux/macOS game zips
alwaysApply: true
---

# FactoryPlanner “build” = cut the next GitHub release

When the user says **build** (or “ship it”, “release”, “publish” in that same sense):

1. **Diff since last release tag** (`git describe --tags --abbrev=0` / `git log <last>..HEAD`).
2. **Summarize** the changes into a short title + notes in the same style as older releases:
   - Title: `vX.Y.Z — short summary`
   - Notes: short markdown sections (`## Multiplayer`, `## Fix`, etc.) like `v0.2.9 — host code appears instantly`.
3. **Bump** `Cargo.toml` patch version (e.g. `0.3.2` → `0.3.3`) unless they named a version.
4. **Commit** all intended project changes (not junk `src/*.rs:line` stubs, not secrets).
5. **Publish immediately**:
   - Push `main`
   - Create the GitHub Release **first** with title + notes (`gh release create`) so Assets isn’t empty of meaning
   - Tag `vX.Y.Z` so `.github/workflows/release.yml` builds and uploads **Steam-style zips** (binary + `assets/`, never source) for:
     - `factory_planner-linux-x86_64.zip`
     - `factory_planner-windows-x86_64.zip`
     - `factory_planner-macos-aarch64.zip`
     - `factory_planner-macos-x86_64.zip`
6. **Do not** ship source in the release. Package layout inside each zip:

   ```
   FactoryPlanner/
     factory_planner(.exe)
     assets/
       belts/ buildings/ data/ environment/ icons/ items/ …
   ```

7. Reply with the release URL. Don’t make the user wait on a silent watch unless they ask.

Preferred order that avoids “tag but no Release / only Source code zip”:

```bash
# after commit + version bump on main:
git push origin main
gh release create "vX.Y.Z" \
  --title "vX.Y.Z — <summary>" \
  --notes "$(cat <<'EOF'
## …
EOF
)" \
  --latest \
  --target main
```

`gh release create` creates/pushes the tag; CI then attaches the four game zips. Softprops must use `generate_release_notes: false` so it doesn’t wipe the titled notes.

Players unzip and run the binary from inside the folder (so `assets/` sits next to the exe). Same layout Steam will use later.
